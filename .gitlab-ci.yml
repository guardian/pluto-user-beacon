image: docker:19.03.11

services:
  - docker:dind

build:
  stage: build
  script:
    - docker build . -t guardianmultimedia/pluto-user-beacon:$CI_JOB_ID
    - docker push guardianmultimedia/pluto-user-beacon:$CI_JOB_ID